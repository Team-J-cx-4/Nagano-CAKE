<div class='container'>
  <div class='row'>
    <div class="p-3 mb-2 bg-light text-dark">注文履歴一覧</div>
    <table class='table'>
      <thead>
        <tr>
          <th>購入日時</th>
          <th>購入者</th>
          <th>注文個数</th>
          <!--<th>注文ステータス</th>-->
        </tr>
      </thead>
      <tbody>
        <% @orders.each do |order| %>
          <tr>
            <td><%=  link_to order.created_at.strftime("%Y-%m-%d %H:%M:%S"),edit_admin_order_path(order) %></td>
            <td><%= order.customer.last_name + "　" + order.customer.first_name %></td>
            <td><%= order.order_details.sum(:amount) %></td>
            <!--<td><% if order.status == false %>-->
            <!--  <strong class="text-success">有効</strong>-->
            <!--<% else %>-->
            <!--  <strong class="text-secondary">無効</strong>-->
            <!--<% end %>-->
            <!--</td>-->
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>